\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{cronologicug}
\author{cronologic GmbH \& Co KG}

%\DeclareOption*{\newcommand{\deviceName}{\CurrentOption}}

\PassOptionsToPackage{utf8}{inputenc}
\RequirePackage{inputenc}
\PassOptionsToPackage{usenames, dvipsnames}{xcolor}
\RequirePackage{xcolor}
\RequirePackage{graphicx} %for including figures
\RequirePackage{tabularx}
\RequirePackage{scrhack}  % avoid float@addtolist warning in package "listing"
\RequirePackage{amsmath} %for math environments
\RequirePackage{siunitx}  % SI Unit package
\PassOptionsToPackage{left=25mm, right=25mm, top=25mm, bottom=25mm}{geometry}
\RequirePackage{geometry} %for page properties (pagestyle)
\PassOptionsToPackage{section,subsection,subsubsection}{extraplaceins}
\RequirePackage{extraplaceins} %for binding figures to a (sub-)section
\RequirePackage{listings}%for code in latex document, might be the source of the float@addtolist warning
\RequirePackage{textcomp}%for code in latex document
\RequirePackage{balance}%for two-column layout
\RequirePackage{array} %for centered AND constant width columns
\RequirePackage{longtable}
\PassOptionsToPackage{footsepline=1pt}{scrlayer-scrpage}
\RequirePackage{scrlayer-scrpage}%for header, footer
\RequirePackage{makecell}  % multi line table cells
\PassOptionsToPackage{T1}{fontenc}
\RequirePackage{fontenc}  % fix warning for availability of bold slanted fonts
\RequirePackage{fontspec}
\RequirePackage{attachfile2} 

% \usepackage{auto-pst-pdf} % this enables postscript graphics for PDF

\RequirePackage{incgraph} % to include the title page

% double line border support
\RequirePackage{tikz}
\usetikzlibrary{calc}
\RequirePackage{eso-pic}

\PassOptionsToPackage{colorlinks=true,linkcolor=blue}{hyperref}
\RequirePackage{hyperref}
 
%%%%%%%%%% Defitinions for the whole document 
 
% color definitions
\definecolor{listinggray}{gray}{0.9}  
\definecolor{lbcolor}{rgb}{0.9,0.9,0.9}
\definecolor{crondarkgreen}{RGB}{32, 90, 133} %1. Überschrift
\definecolor{crondarkblue}{RGB}{43, 119, 177} %2. Überschrift
\definecolor{cronlightgreen}{RGB}{86,159,214} %3. Überschrift
\definecolor{crongrey}{gray}{0.32} %Variablendeklaration API
%
%
\addtokomafont{chapter}{\color{crondarkgreen}}
\addtokomafont{section}{\color{crondarkblue}}
\addtokomafont{subsection}{\color{cronlightgreen}}
%

%import a title PDF and create TOC
\newcommand{\cronofront}[1]{
	\pagenumbering{Alph}   %this is invisible but avoids a warning for duplicate hyperlink targets
	\newpage
	\pagestyle{empty}
	\incgraph[a4paper][width=\paperwidth]{#1}
	\newpage     
	\tableofcontents
	\newpage
	\pagestyle{scrheadings}
	\pagenumbering{arabic}
}

\newcommand{\filesep}{\textbackslash\allowbreak}  % used in file pathes
\newcommand{\lb}{\linebreak}
\newcommand{\cronvar}[2]{\noindent\textsf{\textbf{\textcolor{crongrey}{#1}~#2}}}%for variable declaration
\newcommand{\crondef}[1]{\noindent\textsf{\textcolor{crongrey}{\#{}define} #1}}%for definitions
\newcommand{\tu}{\textunderscore}%shorter
\newcommand{\subscript}[1]{\ensuremath{_{\textrm{#1}}}}
\renewcommand{\arraystretch}{1.6} 
\parskip 8pt



%%%%%%%%%%% Design Footer 
% https://esc-now.de/_/latex-individuelle-kopf--und-fusszeilen-update
\setkomafont{footsepline}{\color{cronlightgreen}}
\pagestyle{scrheadings}
\clearpairofpagestyles
\ifoot{cronologic GmbH \& Co. KG}
\cfoot{\pagemark}
\ofoot{\deviceName\ User Guide \ugrev} 
\renewcommand*{\chapterpagestyle}{scrheadings} % The first page of a chapter should not look different
 
